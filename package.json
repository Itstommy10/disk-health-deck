{
  "name": "disk-health-deck",
  "version": "1.0.0",
  "private": true,
  "description": "Stream Deck plugin: disk space, SMART health, temperature, I/O, network, fans and power.",
  "type": "module",
  "scripts": {
    "build": "node build-icons.mjs && node build-profile.mjs",
    "gallery": "node build-gallery.mjs",
    "validate": "streamdeck validate com.diskdeck.monitor.sdPlugin",
    "pack": "npm run build && streamdeck pack com.diskdeck.monitor.sdPlugin --output dist --force",
    "link": "streamdeck link com.diskdeck.monitor.sdPlugin",
    "restart": "streamdeck restart com.diskdeck.monitor"
  }
}
